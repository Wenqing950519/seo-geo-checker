/ /home.html 200

# NOTE: this file is shared. It configures Cloudflare Pages *and* the B API
# Worker, whose assets directory is this same folder. A rule written for one
# origin therefore also applies to the other, so keep every rule path-only and
# origin-agnostic. Cross-origin links belong in the HTML, not here.
/developers/docs /developers-docs.html 200
/developers-docs /developers/docs 308
