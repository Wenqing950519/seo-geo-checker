---
name: GeoCheck & Platform (LS-Labs family)
description: 同一家族、各有個性——GeoCheck 是「儀器與檢測單」，Platform 是「以真實 request/response 為證的開發者平台」。
colors:
  navy-field: "#0B1F3B"
  navy-deep: "#060E1C"
  slate-ink: "#1E293B"
  slate-muted: "#475569"
  slate-faint: "#64748B"
  on-navy: "#F8FAFC"
  on-navy-body: "#CBD5E1"
  on-navy-muted: "#94A3B8"
  paper: "#FFFFFF"
  hairline: "#E2E8F0"
  lab-blue: "#1D4ED8"
  lab-blue-strong: "#1E40AF"
  signal-blue: "#60A5FA"
  sweep-edge: "#93C5FD"
  lab-blue-tint: "#EFF6FF"
  desk-grey: "#EEF2F7"
  slip-rule: "#D5DDE8"
  flag-yes-text: "#047857"
  flag-yes-tint: "#ECFDF5"
  flag-part-text: "#B45309"
  flag-part-tint: "#FFF4E0"
  flag-no-text: "#B91C1C"
  flag-no-tint: "#FEF2F2"
  flag-unknown-text: "#475569"
  flag-unknown-tint: "#F1F5F9"
  status-amber: "#F59E0B"
  platform-indigo: "#4F46E5"
  platform-indigo-strong: "#4338CA"
  platform-indigo-on-navy: "#A5B4FC"
  platform-indigo-tint: "#EEF2FF"
  platform-wash: "#F6F7FB"
  platform-line-strong: "#CBD5E1"
  code-bar: "#0A1A33"
  code-text: "#E2E8F0"
  code-muted: "#94A3B8"
  syntax-keyword: "#C4B5FD"
  syntax-string: "#A7F3D0"
  syntax-comment: "#7C8DA6"
  syntax-function: "#93C5FD"
  syntax-number: "#FCD34D"
  engine-openai: "#10A37F"
  engine-gemini: "#4285F4"
  engine-anthropic: "#CC785C"
  engine-perplexity: "#22B8CD"
  console-sidebar: "#0A1628"
  console-card: "#0E1D36"
  console-card-border: "#1C2E4D"
  console-text-secondary: "#A9B7CB"
  console-text-muted: "#8FA2C6"
typography:
  display:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "clamp(2.35rem, 6.2vw, 4.5rem)"
    fontWeight: 900
    lineHeight: 1.16
    letterSpacing: "-0.01em"
  display-platform:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "clamp(2rem, 4vw, 3.1rem)"
    fontWeight: 900
    lineHeight: 1.15
    letterSpacing: "-0.015em"
  headline:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "clamp(1.6rem, 3.2vw, 2.25rem)"
    fontWeight: 900
    lineHeight: 1.25
    letterSpacing: "-0.01em"
  headline-platform:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "clamp(1.5rem, 3vw, 2.1rem)"
    fontWeight: 900
    lineHeight: 1.25
    letterSpacing: "-0.01em"
  title:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "1.15rem"
    fontWeight: 900
    lineHeight: 1.25
    letterSpacing: "0.04em"
  body:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "17px"
    fontWeight: 400
    lineHeight: 1.75
  body-platform:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "16px"
    fontWeight: 400
    lineHeight: 1.7
  numeral:
    fontFamily: "Space Grotesk, Noto Sans TC, system-ui, sans-serif"
    fontSize: "1.05rem"
    fontWeight: 700
    lineHeight: 1.4
    fontFeature: "\"tnum\" 1"
  numeral-lead:
    fontFamily: "Space Grotesk, Noto Sans TC, system-ui, sans-serif"
    fontSize: "2.4rem"
    fontWeight: 700
    lineHeight: 1.1
    fontFeature: "\"tnum\" 1"
  wordmark:
    fontFamily: "Space Grotesk, Noto Sans TC, system-ui, sans-serif"
    fontSize: "1.12rem"
    fontWeight: 700
    lineHeight: 1
    letterSpacing: "-0.01em"
  data:
    fontFamily: "IBM Plex Mono, ui-monospace, SFMono-Regular, Menlo, monospace"
    fontSize: "0.78rem"
    fontWeight: 400
    lineHeight: 1.6
  code:
    fontFamily: "IBM Plex Mono, ui-monospace, SFMono-Regular, Menlo, monospace"
    fontSize: "0.82rem"
    fontWeight: 400
    lineHeight: 1.7
  label:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "0.74rem"
    fontWeight: 500
    lineHeight: 1.4
    letterSpacing: "0.08em"
  flag:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "0.76rem"
    fontWeight: 700
    lineHeight: 1.6
    letterSpacing: "0.04em"
  button:
    fontFamily: "Noto Sans TC, PingFang TC, Microsoft JhengHei, system-ui, sans-serif"
    fontSize: "0.95rem"
    fontWeight: 700
    lineHeight: 1
    letterSpacing: "0.01em"
rounded:
  ruled: "0px"
  slip: "4px"
  control: "6px"
  button: "8px"
  panel: "10px"
  pill: "999px"
spacing:
  gutter: "16px"
  cell-y: "14px"
  cell-x: "28px"
  cell-x-mobile: "18px"
  section-geocheck: "clamp(72px, 10vw, 128px)"
  section-platform: "clamp(64px, 9vw, 112px)"
  wrap-geocheck: "1120px"
  wrap-platform: "1180px"
  measure-geocheck: "42rem"
  measure-platform: "44rem"
components:
  button-geocheck:
    backgroundColor: "{colors.lab-blue}"
    textColor: "{colors.paper}"
    typography: "{typography.button}"
    rounded: "{rounded.button}"
    padding: "0 20px"
    height: "44px"
  button-geocheck-hover:
    backgroundColor: "{colors.lab-blue-strong}"
    textColor: "{colors.paper}"
  button-platform:
    backgroundColor: "{colors.platform-indigo}"
    textColor: "{colors.paper}"
    typography: "{typography.button}"
    rounded: "{rounded.button}"
    padding: "0 16px"
    height: "40px"
  button-platform-hover:
    backgroundColor: "{colors.platform-indigo-strong}"
    textColor: "{colors.paper}"
  button-platform-ghost:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.navy-field}"
    rounded: "{rounded.button}"
    padding: "0 16px"
    height: "40px"
  button-platform-ghost-hover:
    backgroundColor: "{colors.platform-wash}"
    textColor: "{colors.navy-field}"
  probe-input:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.navy-field}"
    typography: "{typography.data}"
    rounded: "{rounded.panel}"
    padding: "6px"
    height: "52px"
  result-slip:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.slate-ink}"
    rounded: "{rounded.slip}"
    padding: "14px 28px"
  flag-yes:
    backgroundColor: "{colors.flag-yes-tint}"
    textColor: "{colors.flag-yes-text}"
    typography: "{typography.flag}"
    rounded: "{rounded.slip}"
    padding: "2px 8px"
  flag-part:
    backgroundColor: "{colors.flag-part-tint}"
    textColor: "{colors.flag-part-text}"
    typography: "{typography.flag}"
    rounded: "{rounded.slip}"
    padding: "2px 8px"
  flag-no:
    backgroundColor: "{colors.flag-no-tint}"
    textColor: "{colors.flag-no-text}"
    typography: "{typography.flag}"
    rounded: "{rounded.slip}"
    padding: "2px 8px"
  flag-unknown:
    backgroundColor: "{colors.flag-unknown-tint}"
    textColor: "{colors.flag-unknown-text}"
    typography: "{typography.flag}"
    rounded: "{rounded.slip}"
    padding: "2px 8px"
  flag-sample:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.navy-field}"
    typography: "{typography.flag}"
    rounded: "{rounded.slip}"
    padding: "2px 8px"
  nav-link-geocheck:
    textColor: "{colors.slate-ink}"
    rounded: "{rounded.control}"
    padding: "0 12px"
    height: "40px"
  nav-link-geocheck-active:
    backgroundColor: "{colors.lab-blue-tint}"
    textColor: "{colors.lab-blue}"
  nav-link-platform:
    textColor: "{colors.slate-muted}"
    rounded: "{rounded.control}"
    padding: "0 10px"
    height: "36px"
  nav-link-platform-active:
    backgroundColor: "{colors.platform-wash}"
    textColor: "{colors.navy-field}"
  code-panel:
    backgroundColor: "{colors.navy-field}"
    textColor: "{colors.code-text}"
    typography: "{typography.code}"
    rounded: "{rounded.panel}"
    padding: "20px 22px"
  code-tab:
    textColor: "{colors.code-muted}"
    typography: "{typography.code}"
    padding: "0 12px"
    height: "40px"
  code-tab-selected:
    textColor: "{colors.paper}"
  status-pill:
    backgroundColor: "{colors.paper}"
    textColor: "{colors.slate-muted}"
    rounded: "{rounded.pill}"
    padding: "4px 10px"
  docs-sidebar-link-active:
    backgroundColor: "{colors.platform-indigo-tint}"
    textColor: "{colors.platform-indigo-strong}"
    rounded: "{rounded.control}"
    padding: "6px 10px"
---

# Design System: GeoCheck & Platform (LS-Labs family)

本檔記錄 D-054（2026-09-25）重構後**實際建成**的視覺系統。範圍是兩個兄弟產品的公開頁面：GeoCheck（`geocheck.lslabs.tw`）與 Platform（`platform.lslabs.tw`）。Dashboard（`apps/web/app/`，`app.lslabs.tw`）不屬於本系統。

token 的來源分三層，frontmatter 是規範值：

| 層 | 檔案 | 前綴 |
|---|---|---|
| 家族層（LS-Labs） | `apps/web/public/assets/tokens.css` | `--ls-*`（含 D-054 新增 `--ls-platform-*`） |
| GeoCheck 產品層 | `apps/web/public/assets/geocheck.css`；`/report` 由 `services/api/server.js` 的 `realLiteReportHtml` 以內嵌 CSS 鏡像同一組值 | `--gc-*` |
| Platform 產品層 | `apps/web/public/assets/platform.css`、`platform.js`；Console 在 `developers-console.html` 內自帶變數 | `--pf-*`（Console 內為 `--gc-*` 舊名，見 Don'ts） |

## Overview

**Creative North Star: "The Instrument and Its Slip"（GeoCheck）／"The Request Is the Proof"（Platform）**

家族層只有一個承諾：同一片 navy 夜色（`navy-field` #0B1F3B）、同一組字體（Noto Sans TC、Space Grotesk、IBM Plex Mono，皆為 LS-Labs 母品牌指定字體）、同一套 slate 中性色與 1px 細線。兩個產品在這個底上各自有一個主視覺與一個行動色，互不借用。

GeoCheck 是「儀器與它印出的檢測單」。navy 場域裡只有一座細線雷達與一道藍色掃描；它產出的是一張白色、方角、有印刷格線的檢測單（簡化版實驗室報告）：每列是「項目／結果／判讀」，判讀用一個小旗標（有／部分／無／未知），報告編號與日期用等寬字。LS 藍（`lab-blue` #1D4ED8）是唯一行動色。它明確拒絕發光的深色 SaaS hero、漂浮晶片與分數儀表。

Platform 是把類別標準做到位的開發者平台（以 Stripe／Resend／Vercel 文件站的精緻度為基準）：白與冷灰紙面、navy 墨色、每個畫面一塊 navy 程式碼面板，證據就是真實的 request 與四引擎 response。LS 靛色（`platform-indigo` #4F46E5）是 Platform 自己的行動色。Console 是唯一的深色例外：它被視為「程式碼面板的內部」，沿用同一套色、字與靛色行動，但不發光。

**Key Characteristics:**
- 一片 navy 基底、兩個行動色：GeoCheck 藍、Platform 靛，各守各的站。
- 資料永遠有字體分工：中文用 Noto Sans TC，數字用 Space Grotesk 等寬數字，編號／日期／程式碼用 IBM Plex Mono。
- 以 1px 細線與紙面層次分區；陰影只用在「浮起的紙」與「程式碼面板」。
- 「未知」在視覺上與「0」和「無」都不同：灰色、空心圓點。
- 動態極少：GeoCheck 只有一道雷達掃描；其他都是短暫的狀態轉場，並尊重 reduced motion。

## Colors

一片 navy、一套 slate、每個產品一個行動色，外加四色判讀旗標與四色引擎識別。對比值取自 `tokens.css` 的 WCAG 2.1 實測註記（2026-09-13）；標「本檔計算」者為撰寫本檔時以同一公式計算。

### Primary
- **LS Lab Blue**（#1D4ED8，`--ls-accent-on-light` → `--gc-action`）：GeoCheck 唯一的行動色，用於按鈕、連結、焦點框、公式數字。白字在其上 6.70:1。hover／按下改用 **Lab Blue Strong**（#1E40AF，8.72:1）。
- **Signal Blue**（#60A5FA，`--ls-accent-on-dark` → `--gc-signal`）：navy 場域上的藍——標題強調字、進度條、深色區連結與焦點框（on navy 6.49:1，本檔計算）。雷達掃描以它的透明度漸層繪成，最前緣帶一點 **Sweep Edge**（#93C5FD）。
- **Lab Blue Tint**（#EFF6FF）：GeoCheck 導覽 hover／active 底色、`notice` 底色。

### Secondary
- **Platform Indigo**（#4F46E5，`--ls-platform-fill` → `--pf-accent`）：Platform 與 Console 的唯一行動色，白字 6.29:1；hover 用 **Indigo Strong**（#4338CA，7.90:1），Platform 內文連結也用 Indigo Strong。
- **Indigo on Navy**（#A5B4FC，`--ls-platform-on-dark`）：程式碼面板內已選分頁的底線、面板內焦點框（on navy 8.27:1）。
- **Indigo Tint**（#EEF2FF）：docs 側欄目前頁、`callout.info` 與法務頁 `notice` 底色。

### Tertiary（判讀旗標與狀態）
- **有**：文字 #047857 on 底 #ECFDF5（5.21:1）。
- **部分**：文字 #B45309 on 底 #FFF4E0（4.61:1）。
- **無**：文字 #B91C1C on 底 #FEF2F2（5.91:1）。
- **未知**：文字 #475569 on 底 #F1F5F9（6.92:1），圓點為空心。
- **Status Amber**（#F59E0B）：Platform 狀態膠囊前的圓點（目前狀態：Private Beta）。
- **引擎識別色**：OpenAI #10A37F、Gemini #4285F4、Anthropic #CC785C、Perplexity #22B8CD。只出現在 Platform 程式碼範例與 Console 的引擎小圓點；它們是身分，不是裝飾。

### Neutral
- **Navy Field**（#0B1F3B，`--ls-navy`）：GeoCheck 儀器場域、結尾區、`/report` 頂列、404 底；Platform 程式碼面板底；兩站所有標題墨色（on white 16.49:1，本檔計算）。
- **Navy Deep**（#060E1C）：兩站頁尾、GeoCheck 場域漸層的外圈、Console 畫布。
- **Slate Ink**（#1E293B）：內文（on white 14.63:1）。**Slate Muted**（#475569）：次要文字（7.58:1）。**Slate Faint**（#64748B）：表頭、麵包屑等最小仍達 AA 的淺字（on white 4.76:1）。
- **On Navy**（#F8FAFC）、**On Navy Body**（#CBD5E1，on navy 11.11:1，本檔計算）、**On Navy Muted**（#94A3B8，on navy 6.43:1，本檔計算）：深色面上的三階文字。
- **Paper**（#FFFFFF）：檢測單、Platform 紙面。**Desk Grey**（#EEF2F7，`--gc-desk`）：檢測單所躺的桌面、`/report` 背景。**Platform Wash**（#F6F7FB，`--pf-wash`）：Platform 交替區塊、hero 漸層起點、ghost hover、行內 code 底。
- **Slip Rule**（#D5DDE8，`--gc-rule`）：檢測單外框與印刷格線。**Hairline**（#E2E8F0）：列與列之間最輕的線。**Line Strong**（#CBD5E1，`--pf-line-strong`）：Platform ghost 按鈕框、表格頭線。
- **程式碼面板**：分頁列 **Code Bar**（#0A1A33）、內文 #E2E8F0（on navy 13.38:1，本檔計算）、次要 #94A3B8（on code bar 6.78:1，本檔計算）；語法色 keyword #C4B5FD、string #A7F3D0、comment #7C8DA6（on navy 4.88:1，本檔計算）、function #93C5FD、number #FCD34D。
- **Console 深色層**：側欄 #0A1628、卡片 #0E1D36、卡片框 #1C2E4D、次要文字 #A9B7CB（on canvas 9.49:1，本檔計算）、弱化文字 #8FA2C6（on card 6.53:1，本檔計算）。

### Named Rules
**The One Action Colour Rule.** 每個產品只有一個行動色：GeoCheck 是 LS 藍（#1D4ED8），Platform 與 Console 是 LS 靛（#4F46E5）。不在 GeoCheck 用靛色，不以 LS 藍當 Platform 的按鈕。

**The On-Light / On-Dark Rule.** 顏色依「放在什麼底上」取用，不依色相取用。亮面文字用 `-on-light` 階；navy 上用 `-on-dark` 階。#3B82F6 當白底文字或承載白字都只有 3.68:1，一律不用；#94A3B8 在白底只有 2.56:1，只准出現在 navy 上；#64748B 在 `--ls-void` 上只有 4.14:1、在 navy 上 3.47:1、在 Desk Grey 上 4.23:1（後兩者本檔計算），只准出現在白紙上。

**The Green Is a Status Rule.** 綠色只表示「有／成功」，永遠不是行動。所有互動由行動色承擔。

**The Unknown Is Not Zero Rule.** 「未知」有自己的灰色與空心圓點，不得借用「無」的紅，也不得顯示為 0。

## Typography

**Display Font:** Noto Sans TC（後備 PingFang TC、Microsoft JhengHei、system-ui）
**Body Font:** Noto Sans TC（同上）
**Numeral / Wordmark Font:** Space Grotesk（後備 Noto Sans TC、system-ui）
**Label/Mono Font:** IBM Plex Mono（後備 ui-monospace、SFMono-Regular、Menlo）

三款皆為 LS-Labs 母品牌指定字體，兩站以同一條 Google Fonts 請求載入：IBM Plex Mono 400/500、Noto Sans TC 400/500/700/900、Space Grotesk 500/700。

**Character:** 中文標題用 Noto Sans TC 900 的重量感說話，數字交給 Space Grotesk 的等寬數字讓表格對齊，而所有「可被核對的東西」（網址、報告編號、日期、程式碼）都用 IBM Plex Mono，讀起來像儀器印出的字。

### Hierarchy
- **Display**（900，clamp(2.35rem, 6.2vw, 4.5rem)，1.16）：GeoCheck 首頁 navy 場域的兩行標題，強調詞改用 Signal Blue、不用斜體。
- **Display Platform**（900，clamp(2rem, 4vw, 3.1rem)，1.15，-0.015em）：Platform 首頁 hero 標題。
- **Headline**（900，clamp(1.6rem, 3.2vw, 2.25rem)，1.25）：GeoCheck 區塊標題；Platform 版本為 clamp(1.5rem, 3vw, 2.1rem)。閱讀頁 h1 為 clamp(1.8rem, 4vw, 2.4–2.6rem)。
- **Title**（900，1.15rem，字距 0.04em）：檢測單抬頭「AI 能見度檢測單」；`/report` 各段 h2（1.05rem，0.04em）同一語氣。
- **Body**（400，17px，1.75）：GeoCheck 內文。Platform 為 16px／1.7。閱讀欄寬 GeoCheck 42rem、Platform 44rem；引言 lede 限 34–40em。
- **Numeral**（Space Grotesk 700，1.05rem，tabular-nums）：檢測單結果欄、價格、步驟編號；`/report` 主分數放大為 2.4rem（手機 2rem）。
- **Data**（IBM Plex Mono 400，0.78rem，1.6）：檢測單的受檢網站、題數、報告編號；首頁網址輸入框（1rem）；文件日期（0.84rem）。
- **Code**（IBM Plex Mono，0.82rem，1.7）：程式碼面板；Platform hero 內縮為 0.78rem，520px 以下 0.7rem 並折行。
- **Label**（500，0.74rem，字距 0.08em，Slate Faint）：檢測單表頭「項目／結果／判讀」；Platform 狀態表表頭為 0.78rem／0.06em。
- **Wordmark**（Space Grotesk 700，1.12rem，-0.01em）：「GeoCheck」「Platform」字標；旁邊的「by LS-Labs」為 Space Grotesk 500 0.72rem。

### Named Rules
**The Three Voices Rule.** 文字、數字、證據三種聲音各用一款字：Noto Sans TC 說話、Space Grotesk 計數、IBM Plex Mono 記錄。不在內文段落中使用等寬字，也不用 Space Grotesk 排中文。

**The Tabular Numbers Rule.** 任何會上下比對的數字（分數、百分比、題數、價格）都開 `font-variant-numeric: tabular-nums`。

## Layout

- **容器**：兩站都是 `min(100% - 32px, 寬度)`，即手機左右 16px 邊距、無水平捲動。GeoCheck 1120px、Platform 1180px、docs 1240px、`/report` 880px。
- **區塊節奏**：GeoCheck 區塊上下 clamp(72px, 10vw, 128px)；Platform clamp(64px, 9vw, 112px)。區塊之間用底色（Paper ↔ Desk Grey／Platform Wash）交替，而不是卡片。
- **GeoCheck 首頁**：第一屏是置中的 navy 場域（`min(820px, 100svh)`），雷達放在兩行標題後方，網址輸入框與「開始檢查」在中央；下方 Desk Grey 區的範例檢測單以 −120px（880px 以下 −96px）往上疊進場域邊緣，標記「範例」。範例區是 1.45fr／1fr 雙欄，880px 以下單欄。
- **檢測單**：格子內距 14px 28px；640px 以下改為 12px 18px，表頭隱藏，每列變成「項目＋結果」兩欄、判讀說明換到下一行。
- **Platform 首頁**：左文右碼雙欄（1.1fr／1fr），960px 以下單欄；原則區 2 欄，640px 以下單欄；狀態表 640px 以下每列變成堆疊區塊。
- **Docs**：15.5rem 固定側欄（sticky，top 61px）＋ 最寬 52rem 內容欄；900px 以下側欄變成頁首的可展開目錄（`<details>`），展開後兩欄排列。
- **導覽列**：GeoCheck 高 64px，820px 以下只留 `nav-keep` 項目與行動按鈕；Platform 高 60px，860px 以下隱藏文字連結，420px 以下再隱藏 ghost 按鈕與副標。

## Elevation & Depth

系統以紙面層次與細線為主，陰影只有兩種用途：讓「紙」浮在桌面或場域上，以及讓 navy 程式碼面板浮在白紙上。陰影一律是大範圍、負擴散、帶 navy 色的柔影，沒有硬邊陰影、沒有發光。

### Shadow Vocabulary
- **Slip lift**（`box-shadow: 0 1px 0 #D5DDE8, 0 24px 48px -28px rgba(11, 31, 59, .35)`）：檢測單；`/report` 的整張單為 `0 1px 0 #D5DDE8, 0 28px 56px -32px rgba(11,31,59,.4)`。第一層 1px 是紙張的下緣。
- **Probe float**（`box-shadow: 0 24px 60px -20px rgba(0, 0, 0, .55)`）：navy 場域上的白色網址輸入框。
- **Code panel**（`box-shadow: 0 30px 60px -30px rgba(11, 31, 59, .55)`）：Platform 首頁的 navy 程式碼面板。docs 內的程式碼區塊不帶陰影。
- **Modal**（`box-shadow: 0 32px 80px -24px rgba(0, 0, 0, .6)`）：GeoCheck 設定視窗；遮罩為 rgba(6, 14, 28, .62)。
- **Toast**（`box-shadow: 0 16px 40px -16px rgba(0, 0, 0, .5)`）：Platform 複製提示。

### Named Rules
**The Paper Lifts, Nothing Glows Rule.** 只有紙（檢測單、輸入框、視窗）與程式碼面板可以有陰影；Console 的卡片、統計框、方案卡陰影為 none，以 1px 框線分層。

**The Sticky Glass Rule.** 捲動時的導覽列是唯一使用 backdrop-filter 的元素（白 92–96% 透明度＋`saturate(1.4) blur(10px)`）。

## Shapes

- **方角是紙**：檢測單與判讀旗標 4px，近乎方角，像印出來的單據。GeoCheck 的方案卡、白皮書流程卡、步驟與比較表採 0 圓角，靠**頂端 2px 墨色線**（#0B1F3B）當抬頭；步驟用 1px 墨線。
- **控制項 6px**：導覽連結、docs 側欄連結、notice、GeoCheck 視窗內的輸入與摺疊框、複製按鈕。
- **按鈕 8px**：兩站主要按鈕；Platform 以 `--pf-radius: 8px` 統一按鈕、callout、toast。
- **面板 10px**：navy 程式碼面板與 GeoCheck 網址輸入框外殼。
- **膠囊 999px**：只用於 Platform 狀態膠囊。
- **圓點**：旗標前 6px 實心圓（未知為 7px 空心、1.5px 邊）；引擎識別 7px 圓點。
- **線的層級**：一般分隔 1px Hairline；檢測單格線 1px Slip Rule；檢測單抬頭下緣與 `/report` 抬頭為 2px 墨線；Platform 表頭 1px Line Strong。

## Components

### Buttons
- **Shape:** 8px 圓角，無框（Platform ghost 除外）。
- **GeoCheck primary:** Lab Blue 底、白字、700 0.95rem；高 44px、左右 20px。網址輸入框內與結尾區放大到 52px 高、24px 左右。hover 換 Lab Blue Strong；按下 translateY(1px)；停用（檢查中）底色 #7C8BA1、游標 progress。
- **Platform primary:** Platform Indigo 底、白字、700 0.9rem；高 40px、左右 16px；大尺寸 46px／20px。hover 換 Indigo Strong。停用 opacity .55。
- **Platform ghost:** 白底、navy 字、1px Line Strong 框；hover 換 Platform Wash 底、框轉 Slate Faint。
- **Text link with arrow:** 700 字重的行動色文字＋16px 箭頭，hover 時箭頭右移 3px（GeoCheck `gc-link`、Platform `pf-arrow`）。
- **Focus:** 2px 行動色外框、偏移 2–3px；navy 面上改用 Signal Blue（GeoCheck）或 Indigo on Navy（Platform）。

### Navigation
- **GeoCheck:** 首頁在 navy 場域上以透明底、On Navy 字疊在場域上方（`gc-nav--field`）；捲過 30px 後變成固定的白色毛玻璃列並以 .35s 從上滑入。連結 0.92rem 500，hover／目前頁為 Lab Blue 字＋Lab Blue Tint 底；最右是「免費檢查」主要按鈕（38px 高）。
- **Platform:** 白色 92% 毛玻璃 sticky 列，字標＋文字連結（Slate Muted，hover／目前頁 navy 字＋Wash 底）＋右側「閱讀文件」ghost 與「進入控制台」primary（36px 高）。法務頁在 860px 以下把互連連結排成可橫向捲動的第二列。

### GeoCheck：Navy Radar Field（Signature）
navy 場域以 `radial-gradient(120% 90% at 50% 38%, #10284A, #0B1F3B 46%, #060E1C)` 由中心微亮向外轉深。雷達是一個 SVG：四圈同心圓（第二圈稍亮）與十字準線，線色為 slate 的 8–22% 透明度、線寬 0.12；600px 以下放寬為 0.22 並略提亮度。掃描是一個以圓形遮罩裁切的 conic-gradient，從 300° 開始由透明漸到 Signal Blue 16%，在 359.6° 的前緣達到 Sweep Edge 42%。雷達寬 `min(1180px, 150vw)`（手機 125vw），`aria-hidden`、不接收指標事件。

### GeoCheck：Result Slip（Signature）
白紙、1px Slip Rule 框、4px 圓角、Slip lift 陰影。抬頭：Title 字級的單名＋（樣本時）「範例」旗標，下方一行 IBM Plex Mono 的中繼資料（受檢網站、觀測題數、報告編號），以 2px 墨線結束抬頭。表格三欄：項目（700 墨色，佔 38%，可附 0.84rem 說明）、結果（Numeral）、判讀旗標（靠右）。頁尾是 0.82rem 的 Slate Muted 說明列。`/report` 是同一張單的完整版：navy 頂列＋Desk Grey 上的單張，分段（`part`）以 1px Slip Rule 分隔，主分數 2.4rem，每題可展開原始回答與引用來源，行動建議用 30px 墨色空心圓編號；列印時移除頂列與按鈕、展開所有證據。

### GeoCheck：Result Flags（有／部分／無／未知）
- **Style:** 0.76rem 700、字距 0.04em、2px 8px、4px 圓角；文字色與淡底取自同一狀態的 `-on-light`／`-tint` 對，前方 6px 實心圓點取文字色。
- **未知:** 灰色（#475569 on #F1F5F9），圓點改為 7px 空心圓環——與「無」的紅色與實心點在色與形兩個維度上都不同，色盲下仍可區分。
- **範例（`flag--sample`）:** 透明底、1px 墨色框、墨色字、字距 0.12em、無圓點。只用來標記示範資料，不表示判讀。

### GeoCheck：URL Probe（Input）
白色 10px 外殼、6px 內距、Probe float 陰影，內含 52px 高的 IBM Plex Mono 網址欄與主要按鈕；560px 以下改為上下堆疊、按鈕滿寬。檢查進行中，下方出現 2px 高的進度軌（slate 25%）與 Signal Blue 填滿（.9s 緩出），步驟名以 0.74rem 等寬字列出；錯誤時填色改 #F87171。

### GeoCheck：FAQ Disclosure
原生 `<details>`，上下以 1px Slip Rule 分隔，最寬 50rem。問題列 1.08rem 700 墨色、上下 22px；右側 20px 加號圖示為 Slate Faint，展開時旋轉 45° 變成 ×、轉為 Lab Blue（.3s 緩出）。答案最寬 42em。

### Platform：Navy Code Panel with Tabs + Copy（Signature）
navy 底、10px 圓角、`overflow: hidden`。頂部 Code Bar 列：左邊 IBM Plex Mono 0.8rem 的分頁（curl／TypeScript 等，40px 高，未選 Code Muted、已選白字＋2px Indigo on Navy 底線），右邊 30px 高的「複製」按鈕（1px slate 25% 框，hover 提亮到 50%）。內文 20px 22px、0.82rem、1.7 行高，語法色見 Colors。可加一行 `pf-code-note` 說明「示例資料，不代表即時服務狀態」。分頁以 `role="tab"`／`aria-selected` 切換並支援左右方向鍵；複製的是目前顯示的那一塊，結果以 toast 告知。docs 內的 `code-container` 是同一元件的無陰影版本。

### Platform：Status Table 與 Status Pill
開放狀態以表格呈現（`access`）：表頭 0.78rem 500、字距 0.06em、Slate Faint，下緣 1px Line Strong；列頭 900 墨色、內容 Slate Muted，列與列之間 1px Hairline；640px 以下每列堆疊。狀態膠囊為白底、1px Line Strong 框、999px 圓角、0.8rem Slate Muted 字，前方 7px Status Amber 圓點；只顯示真實狀態。

### Platform：Docs Sidebar & Disclosure
側欄分組標題 0.78rem 700、字距 0.06em、墨色；連結 0.88rem Slate Muted、6px 圓角，hover 為 Wash 底，目前頁為 Indigo Tint 底＋Indigo Strong 700。端點前的方法徽章為 IBM Plex Mono 0.66rem、1px 同色框：POST #047857、GET Indigo Strong、DELETE #B91C1C（520px 以下隱藏）。900px 以下整個側欄收進 `<details>` 目錄，摘要列 44px 高、右側 8px 折角箭頭展開時翻轉。文件表格表頭用 Wash 底、1px Line Strong。

### Platform：Legal Prose
44rem 單欄，h1 clamp(1.8rem, 4vw, 2.4rem) 900，h2 1.25rem 900（上距 2em），段落間距 1.05em，文件日期以 IBM Plex Mono 0.84rem 置於標題下。GeoCheck 的法務與白皮書用同一結構（`prose`，42rem），行內 code 以 Unknown Tint 為底。

### Platform：Console（深色例外）
Console 是程式碼面板的內部：Navy Deep 畫布、#0A1628 側欄、#0E1D36 卡片配 1px #1C2E4D 框、On Navy 主字、#A9B7CB／#8FA2C6 次要字。行動按鈕為 Platform Indigo（hover Indigo Strong），側欄目前項目為 16% 靛色底＋Indigo on Navy 圖示，連結、焦點框與用量進度條都用 Indigo on Navy；計量數字用 Space Grotesk 等寬數字；引擎以四色小圓點標示（無光暈）。圓角沿用 4／6／8／10／999px 階梯。卡片、統計框、方案卡無陰影，以框線分層；精選方案卡只換成靛色框。視窗遮罩 rgba(3, 8, 18, .72)、不模糊。

### Motion
- **雷達掃描**：全站唯一的持續動畫。平時 `9s linear infinite` 一圈；送出檢查後 `body.is-scanning` 讓它加速到 `2.4s`，錯誤時移除。
- **狀態轉場**：共用緩出曲線 `cubic-bezier(0.16, 1, 0.3, 1)`；按鈕 .15–.18s、箭頭與摺疊 .2–.3s、導覽列滑入與視窗升起 .35s、進度條 .9s。
- **Reduced motion**：兩站都把所有動畫與轉場壓到 .01ms、動畫只跑一次，平滑捲動改為 auto——雷達因此靜止。

## Do's and Don'ts

### Do:
- **Do** 先載入 `tokens.css`，再載入產品層 CSS；新值先進 `--ls-*`（家族）或 `--gc-*`／`--pf-*`（產品），以用途命名，不以色相命名。
- **Do** 讓每個產品只有一個行動色：GeoCheck 用 Lab Blue（#1D4ED8），Platform 與 Console 用 Platform Indigo（#4F46E5）。
- **Do** 依底色取用色階：亮面用 `-on-light`，navy 上用 `-on-dark`／Signal Blue／Indigo on Navy。
- **Do** 在 GeoCheck 以「白色檢測單＋項目／結果／判讀＋旗標」呈現結果，數字用 Space Grotesk 等寬數字，編號與日期用 IBM Plex Mono。
- **Do** 把缺資料標成「未知」旗標（灰、空心圓點），並與 0 分和「無」分開呈現。
- **Do** 在任何示範資料上加「範例」旗標。
- **Do** 在 Platform 每個畫面只放一塊 navy 程式碼面板，內容是真實 request／response 結構，並附分頁與複製。
- **Do** 以 1px 細線與底色交替分區；GeoCheck 的卡片型內容用 0 圓角＋頂端 2px 墨線。
- **Do** 保持 16px 手機邊距與無水平捲動；表格在 640px 以下改成堆疊列。

### Don't:
- **Don't** 做發光的深色 SaaS hero、漂浮晶片或分數儀表（GeoCheck 方向契約明確拒絕）。
- **Don't** 在 GeoCheck 加第二座雷達、第二道掃描或其他持續動畫；雷達只在檢查進行中加速。
- **Don't** 把 #3B82F6 當白底文字或讓它承載白字（3.68:1）；不把 #94A3B8 用在亮面（2.56:1）；不把 #64748B 用在 navy 或 Desk Grey 上。
- **Don't** 用綠色當行動色；綠色只代表「有」。
- **Don't** 把四個引擎識別色合併成品牌藍，也不拿它們當裝飾色。
- **Don't** 在 GeoCheck 使用靛色，或在 Platform 使用 LS 藍當按鈕。
- **Don't** 在 Console 加發光陰影或 glow；Console 卡片以框線分層。
- **Don't** 用硬邊位移陰影；陰影只給檢測單、輸入框、視窗、程式碼面板與 toast。
- **Don't** 用狀態膠囊或任何視覺元素暗示未開放的能力（付款、額度）已開放。
