---
type: learning-log
project: GeoCheck
last_updated: 2026-09-09
tags:
  - geocheck
  - learning
---

# GeoCheck Learning Log

> 依 `AGENTS.md` Learning Protocol 與 Anti-Dependency Rule：
> 本檔的目的不是記錄「做了什麼」（那是 git 與 `DECISION_LOG.md` 的工作），
> 而是記錄**你理解了什麼**，以及**還沒理解什麼**。

## 使用方式

每次重要任務結束後，Agent 應建議一則記錄；由你判斷是否真的理解了再寫入。

```markdown
### YYYY-MM-DD 主題

- **情境**：
- **核心概念**：（用自己的話寫，不要複製 Agent 的說法）
- **我能解釋到什麼程度**：能／部分／還不能
- **仍需人工驗證**：
- **下次遇到類似問題我會怎麼做**：
```

## Anti-Dependency 自我檢查

依 Contract，你應該能在沒有 Agent 的情況下說明以下項目。定期自評：

| 項目 | 我能解釋嗎？ | 最後檢查 |
|---|---|---|
| GeoCheck 主要架構（mock-api 各模組職責） | 〔待自評〕 | — |
| V3 演算法為何是 50/30/20 | **不能——因為它本來就沒有依據** | 2026-07-26 |
| 為何 Perplexity 缺結果要標 unknown 而非 0 | 〔待自評〕 | — |
| 資料流：query planner → provider → measurement → 分數 | 〔待自評〕 | — |
| 測試怎麼跑、涵蓋什麼、不涵蓋什麼 | 〔待自評〕 | — |
| GEO 分數的限制與不可宣稱的範圍 | 〔待自評〕 | — |
| usage ledger 為何要與 GEO score 分離 | 〔待自評〕 | — |
| **「品牌在生成式引擎中可見」是什麼意思（不提程式碼）** | **還不能——這是 P2 的目標** | 2026-07-26 |
| **構念 → 操作化 → 效度 的差別** | 〔待自評〕 | 2026-07-26 |
| **量測層與分析層為何要分離** | 〔待自評〕 | 2026-07-26 |
| **可複現性 L1／L2／L3 各是什麼、我現在在哪一層** | 〔待自評〕 | 2026-07-26 |
| **內層權重 40/30/0.3 在哪、影響多大** | 〔待自評〕 | 2026-07-26 |

> 若某項標記為「還不能」，依 Anti-Dependency Rule，下次相關任務應**先補足理解**，而不是繼續堆疊功能。

---

## 記錄

### 2026-07-25 導入 Agent Contract 與 docs/ 事實來源

- **情境**：專案原本沒有 AGENTS.md / CLAUDE.md，研究與決策規範散落在 docx 與對話中。
- **核心概念**：
  - **約束要能被讀到才有效**——Contract 引用的文件若不存在，約束等於失效。
  - **正式決策需要載體**——沒有 DECISION_LOG，「決定過的事」只存在於記憶與對話，無法追溯。
  - **版控範圍決定可追溯性**——被 gitignore 的資料無法支撐「可追溯至資料與版本」的主張。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - `PROJECT_CHARTER.md` 全部 TODO 欄位
  - `DECISION_LOG.md` 中 D-001～D-003 的理由與狀態
  - research-input/ 與 research-output/ 被 gitignore 是否為刻意決定
- **下次遇到類似問題我會怎麼做**：〔待填〕

---

### 2026-07-26 構念先於測量（P0 charter 訪談）

- **情境**：原本只是要補齊 charter 的 13 個 TODO，過程中發現這些 TODO 不是獨立的空格，而是同一個結構性缺口的症狀。訪談結束時決定全面重構，並確立 P0→P1→P2→P3 四階段。

- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：

  1. **構念先於測量。** 順序是 `構念定義 → 操作化 → 變項 → 構面 → 權重 → 實作`。GeoCheck 從第二步開始做，跳過了第一步，所以權重只能憑感覺。這不是疏忽，是順序錯了。
  2. **效度是「測到的是不是你想測的」。** Hunterest 站內準備度 87 卻完全沒被 Perplexity 提及——這不是資料異常，是在提示「站內準備度」可能根本沒在測可見度。
  3. **研究產出與產品產出要分開。** 原始訊號屬研究層（不需要權重），單一分數屬產品層（才需要權重）。現行架構把兩者綁在 `geo-assessment.js`，是權重爭議的結構性根源。
  4. **主動揭露邊界比硬撐結論可信。** A4（引用→獲客）做成探索性分析並列出四項限制，比宣稱因果更有說服力。
  5. **charter 的價值在說真話，不在填滿。** 沒想清楚的欄位標「未定」，比編一個看起來完整的答案更有用。

- **我能解釋到什麼程度**：〔待自評〕

- **仍需人工驗證**：
  - `PROJECT_CHARTER.md §0` 與 `§3.1` 是 Agent 對訪談內容的轉述，**需本人確認沒有走樣**
  - H-001～H-007 全部未驗證，引用時須維持「假設」標記
  - Hunterest vs 壽司郎 為 n=2 線索，**不是結論**
  - D-002（unknown vs 0）與 D-003（DeepSeek 不影響分數）仍為 Proposed
  - 內層權重 `40/30/0.3` 之外還有多少未記錄的隱性決定——**這就是 P1 要查的**

- **下次遇到類似問題我會怎麼做**：〔待填〕

---

### 2026-09-04 全站去 AI 樣板化與純粹中文排版重構

- **情境**：首頁、白皮書與品牌規範手冊充斥常見於 AI 生成（Vibe Coding）的裝飾性全大寫英文字（例如 `WHAT WE MEASURED`、`CORE SIGNAL`、`GOVERNANCE & BOUNDARIES`、`PILLAR 1`），造成中英文混搭割裂與樣板廉價感。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **區分「專業技術名詞」與「裝飾性填充英文」**：具備國際標準的技術詞（`FAQ`、`HTTP`、`JSON-LD`、`Schema.org`、`v1.0.0`）予以保留；但純粹用作卡片頂部的無意義英文眉標屬於視覺噪音，應全面剔除或正名為在地化中文（如「核心推薦信號」、「面向一 · 產品核心指標」）。
  2. **無噪音的視覺層級（Hierarchy without Noise）**：中文版面應由字號、字重、灰階對比與留白節奏建立層次，而非依賴「中文字上方加一行全大寫英文」作為填充物，從而恢復研究型產品的自律與嚴肅感。
  3. **維護研究與商業契約不變**：視覺去樣板化的過程中，嚴格恪守底層合約約束（「這套分數是怎麼算的」、「不會當成 0 分」、65/35 雙軌架構），確保全站 25 組測試套件持續保持 100% 通過。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 各尺寸螢幕下首頁與白皮書的視覺流暢度
  - 語氣與文字節奏是否符合研究型產品的沉穩權威感
- **下次遇到類似問題我會怎麼做**：〔待填〕

---

### 2026-09-09 Wave 0 基準量測與共用網域分層架構

- **情境**：啟動 2026 下半年信義區餐廳 AI 曝光度 Wave 0 基準量測前，需解決「70 家候選中 63 家合格，但去重根網域只有 49 個」的架構問題，並完成零 API 消耗的抓取預檢。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **分析單位兩層分離（店家實體層 vs 網站技術層）**：信義區餐飲業中，多家餐廳共用飯店（如君悅 7 家共用 `hyatt.com`）或商場網域。店家層為 63 家審核合格實體，網站技術層為 49 個去重根網域。必須透過 Entity Master 進行精確門市 URL 映射，絕不可為了湊整數隨意塞入未經審核的孤立網域。
  2. **AI 搜尋的「第三方媒體依賴」現象**：在無品牌推薦情境中，Perplexity 幾乎 100% 引用食尚玩家、窩客島、PopDaily 等第三方整理包，導致官方網站引用率為 0.0%。這實證了 AI Trust Index 將「答案採用（65%）」與「官方來源引用（35%）」拆開衡量的必要性。
  3. **缺失值處理原則（Research Integrity）**：爬取失敗的站點（如被 WAF 攔阻或 DNS 失效），其站內準備度與 AI 分數標記為 `failed/unknown`，嚴禁給 0 分，避免人為扭曲產業統計平均值。
- **我能解釋到什麼程度**：能說明為何 63 家合格門市實體自然對應 49 個獨立根網域，以及為何鼎泰豐在第 1 題被採用但來源引用率為 0% 時計算為 32.5 分（50% × 65% + 0% × 35%）。
- **仍需人工驗證**：
  - 11 家受限站點的 WAF 特徵（萬豪、君悅、海底撈等）是否在 Wave 1 需導入動態渲染。
  - 爬蟲預檢與 API 呼叫分離的成本效益。
- **下次遇到類似問題我會怎麼做**：在資料集凍結前，先透過腳本跑一次 Entity Master 覆蓋率預檢（`assertReviewedEntityCoverage`），確保沒有未審核網域或 URL 協定不一致。

---

### 2026-09-09 題目設計的真實性與消除地板效應（Floor Effect）

- **情境**：Wave 0 初始題組因機械式限制（「晚餐預算 500 到 999 元」）直接排除了信義區高端牛排、景觀與精緻餐廳，導致 94.7% 站點得 0 分；隨後升級為約會慶生、大眾聚餐、火鍋燒肉三大真實消費搜尋情境並重新量測。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **真實搜尋情境才是有效尺標**：消費者向生成式 AI 提問帶有強烈動機（約會浪漫、朋友聚餐、特定品類渴望）。若題目的設定包含人為僵化限制（如偏離受測主體的價格區間），會產生嚴重的「地板效應（Floor Effect）」，導致 95% 樣本集體得 0 分，變異數消失使分組比較完全失效。
  2. **情境化 GEO（Contextual GEO）**：餐廳在 AI 搜尋中的能見度是「分場景」競爭的。題組升級為三大真實情境後，WILDWOOD（約會慶生）、饗食天堂（大眾聚會）、合‧shabu（頂級鍋物）分別在各自所屬的場景破零取得 21.5 分（33% 採用率）。
  3. **跨國品牌的「雙面數位矛盾」**：君悅酒店旗下的「寶艾西餐廳」在約會慶生題中多次被 AI 選為米其林求婚首選（AI 心智極強），但其官方網域 `hyatt.com` 卻部署 Akamai WAF 防火牆阻擋了一般伺服器爬蟲。這生動展現了「品牌聲量深植於外部媒體，但第一方數位資產卻封閉於防火牆後」的真實企業生態。
- **我能解釋到什麼程度**：能解釋為何題組升級至 v2.0 後能消除地板效應，以及為何官方來源引用率依然是 0.0%。
- **仍需人工驗證**：
  - 未破零的頂級牛排館（莫爾頓、教父、S&W），在各情境回傳文本中是否因符號處理（如 `&`）而影響別名命中。
  - 是否需要在正式報告中進一步增加第 4 題商務宴客情境。
- **下次遇到類似問題我會怎麼做**：在確定研究題庫前，先以 1~2 筆代表性站點呼叫 API 觀察真實自然語言在 AI 搜尋引擎中的答案樣貌，確保題目覆蓋不同客單價與料理定位，避免人為製造統計盲點。

---

### 2026-09-10 工具 A 行銷 Dashboard 前端架構與度量真值

- **情境**：在完成工具 A（Product A）後端 Foundation 與 SQLite/D1 schema 後，依據使用者要求打造對標 Ahrefs / GA4 / Brandlight 的現代化行銷 Dashboard 前端，並參照 `Themap` 專案之分頁獨立資料夾目錄模式。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **分頁獨立子目錄架構（Domain-Driven Subfolders）**：各個分析模組（`overview/`、`performance/`、`questions/`、`citations/`、`quality/`、`evidence/`、`auth/`）將視圖與樣式高度封裝，跨模組共用函式、狀態、圖標與驗收情境置於 `shared/`，維持大專案高可維護度與零構建依賴（Zero-Build ES Modules）。
  2. **明確分子分母與未知隔離（Measurement Integrity）**：所有比率（提及率、官網引用率、覆蓋率）皆顯示完整計算基準（如 `87.5% (14/16)`）。`unknown` 項目代表供應商缺損或逾時，嚴格從分母剔除，不與真 0 (`measured=false`) 混淆，更不假造補 0。
  3. **題組版本斷點與不可變性（Immutable Question Sets）**：題組一旦產生即不可變，修改題組必然生成新版本（v1 → v2）。跨版本的走勢圖自動劃設虛線隔離，並凍結跨版 Delta 增減幅計算，防止行銷決策誤將「題型更動」解讀為「品牌成效升降」。
  4. **產品邊界與隔離性（Product Boundaries）**：Product A Dashboard 面向行銷工作者，全站嚴格隔離 Developer API 概念（無 API Key、Tenant、Quota、成本或底層工作 ID），僅透過專屬 `gds_` 會話憑證連線 `/app-api/v1`。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 各瀏覽器在原生 `<script type="module">` 下對 SVG Chart 游標懸停提示與點擊反應之相容性。
  - 在正式 Cloudflare Pages / Workers 部署時，靜態目錄搬移之路由配置。
- **下次遇到類似問題我會怎麼做**：在實作前端時，先寫好包含驗收情境與隔離詞彙掃描的自動化測試（`tests/dashboard-frontend.test.js`），在寫程式與樣式的同時即時驗證規範。

---

### 2026-09-10 商業指標數字字體從代碼等寬轉型 Inter Tabular Figures

- **情境**：使用者直指行銷儀表板「當前最大的問題是數字的字體不對」，並要求自 `brand.html` 規範頁起，一鍵貫穿更新至全站所有頁面。經分析確認原使用之 `JetBrains Mono` 帶有斜線零（`Ø`）與程式碼寬距，破壞了 GA4 / Stripe 等商業分析工具的高階質感。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **代碼字型與商業數據字型的本質差異（Code Monospace vs. Business Tabular）**：IDE 專用字型（如 JetBrains Mono）強調避免字元混淆，刻意加斜線或小點於數字 0，且每個字元強行同寬，造成數字 `1` 兩側出現巨大的空白縫隙。商業分析平台（Stripe、GA4、Ahrefs）則普遍採用現代無襯線體，配合 OpenType 的 `font-variant-numeric: tabular-nums`（`tnum`），既享有比例字型的優美曲線，又具備小數點與欄位絕對垂直對齊的特性。
  2. **Inter 作為現代螢幕顯示標準的優勢**：相較於 Helvetica 在 Windows 系統易退化為 Arial 導致字距失真，`Inter` 專為電腦螢幕高解析度閱讀而生，且原生支援 tabular figures，在 2.35rem 巨型 KPI 記分卡到 0.75rem 走勢圖軸線皆呈現清晰權威的商業質感。
  3. **設計系統字體 Token 的三層職責分離**：
     - `--gc-font-sans`：介面結構、標題、說明文案（Noto Sans TC）。
     - `--gc-font-number`：評分卡、走勢圖刻度、表格數據、百分比（Inter + tabular-nums）。
     - `--gc-font-mono`：API Key、JSON 代碼塊、底層 UUID（JetBrains Mono）。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 在不同解析度螢幕（1080p / 4K / 行動端）下，確認 KPI 大數字與走勢圖座標軸之文字清晰度與垂直對齊感。
- **下次遇到類似問題我會怎麼做**：在初始設計階段即嚴格區分「代碼字體」與「度量數字字體」，避免惰性共用一個 `--font-mono` 導致非技術受眾產生終端機違和感。

---

### 2026-09-10 專業 SaaS 儀表板 UI/UX 與視覺層級優化

- **情境**：儀表板實裝後，使用者反饋 5 個重大體驗問題：走勢圖切換不直觀、KPI 數字過大產生強烈壓迫感且次要標籤折行碰撞、多餘提示橫條干擾、引擎矩陣在一般螢幕跑版切圖且無法滑動、問題矩陣表每列皆顯示相同圖示無法一眼看出各模型推薦差異。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **克制的主數字排版（Calm Typography & Breathing Room）**：巨型數字（如 2.35rem / 38px）在單一畫面上四個並排會造成視覺壓迫與資訊喧賓奪主。專業分析平台（Ahrefs / Stripe / GA4）的 KPI 主數字通常落在 1.65rem ~ 1.8rem（26px ~ 28px），並搭配簡練的自適應次要文字（如 `14/16 次提及`），既保留權威感，又讓介面擁有呼吸感。
  2. **語意化狀態標籤取代裝飾性圖標（Semantic Badges vs. Decorative Icons）**：表格中的引擎圖標若在每列都以全彩形式全部點亮，將失去資訊價值（Information Density 降為 0）。必須引入三態語意區隔（綠色「✓ 提及」、薄荷藍「✓ 引用」、灰色「— 未提及」），行銷人員才能在一秒內看出「Gemini 在這題缺損、ChatGPT 與 Claude 都有推薦」。
  3. **網格防禦與容器橫向滾動（Grid Protection & Responsive Overflow）**：多欄卡片（如 4 引擎卡片）內部若有固定的多欄數據，在 1200px~1366px 筆電螢幕加上側邊欄時，極易發生右側溢出被裁切。外層必須配置獨立的橫向捲動容器（`overflow-x: auto`）並設定網格最小安全寬度（`min-width`），同時加上客製化細捲軸，確保各欄位不擠壓變形且能順暢拖曳檢視。
  4. **統一控制列（Unified Toolbars）**：將檢視模式（整體 vs 四引擎）與過濾圖例整合於圖表頂部，消除模式切換時的垂直版面跳動，並移除「提示：點擊資料點...」等顯而易見的文字橫條，回歸簡潔、沉浸的操作體驗。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 在實體筆電（13 吋／14 吋 1080p 125% 縮放）上實際滑動四引擎矩陣，檢查橫向捲軸流暢度。
  - 檢視問題矩陣表的各引擎狀態膠囊，確認行銷意圖能否第一眼精準解讀。
---

### 2026-09-10 開發者控制台 API 金鑰彈窗防禦排版與克制美學重構

- **情境**：使用者反饋開發者控制台 API 金鑰建立成功彈窗（`#reveal-key-modal`）排版嚴重擠壓，長達 49 字元的 API 金鑰與複製按鈕碰撞折行（出現「複製金」／「鑰」之難看換行），且整體視覺配色過於花俏、充斥 AI 模板感（粉紅警示外框、巨型綠色勾勾、青藍螢光膠囊按鈕），缺乏工程師工具（Stripe、GitHub、Cloudflare、Vercel）應有的樸素克制感。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **長 Token 容器的防禦性排版（Defensive Token Layout & Flex Shielding）**：49+ 字符的等寬金鑰（如 `gck_live_...`）需要約 410px 橫向空間；彈窗若只有 540px 加上 64px 內距，剩餘空間不足以容納複製按鈕，導致按鈕寬度被強制壓縮折行。將卡片上限擴至 `max-width: 640px`，並在按鈕施加 `white-space: nowrap; flex-shrink: 0; min-width: 88px;`，可徹底杜絕任何文字折行。
  2. **工程師產品的克制美學（Developer-Grade Restraint vs. Loud AI Palettes）**：開發者產品講究沈穩、專業與專注代碼，排斥強烈的情緒化配色。移除高彩度的粉紅警告框與綠色徽章，改用現代工程師標準的石板灰暗色系（Slate `#0F172A`、`#1E293B`、`#475569`、`#94A3B8`）搭配微邊框與低對比通知條，營造終端機質感的冷靜信任感。
  3. **複製即時微互動與安全環境降級（Micro-Feedback & Clipboard Fallback）**：點擊「複製金鑰」後立即切換為「已複製 ✓」維持 2 秒並彈出 Toast，提供明確操作回饋；同時針對不支援 `navigator.clipboard` 的非安全上下文（如特定本機開發環境）加入 `execCommand('copy')` 降級容錯，確保金鑰複製永不落空。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 在不同螢幕解析度與瀏覽器環境下開啟建立金鑰彈窗，驗收金鑰完整展示與「已複製 ✓」互動反饋。
- **下次遇到類似問題我會怎麼做**：設計代碼/金鑰展示元件時，預設考慮長字串寬度與按鈕的 `flex-shrink: 0` 防禦，並優先以 Slate 系列冷灰石板色系建構開發者面向的介面，避免預設套用行銷型的高彩度漸層或警示色。

---

### 2026-09-10 開發者平台字體系統與視覺層級全面同步產品 A Dashboard

- **情境**：在完成產品 A 行銷儀表板字體與 UX 優化後，使用者要求開發者相關頁面（`developers-console.html`、`developers.html`、`developers-docs.html`）全面改寫，字體與視覺層級 100% 同步產品 A Dashboard。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **跨產品線的字體系統統一性（Cross-Product Typography Cohesion）**：即便產品 A（行銷儀表板）與 Developer Platform（API 控制台與文件）面對不同客群，全站品牌應當具備一致的高階設計語言。全面採用 `Noto Sans TC` 搭配 OpenType `font-variant-numeric: tabular-nums` 處理商業指標、配額與價格，徹底剔除代碼等寬字型在非代碼區域造成的斜線零（`Ø`）與字距破碎，將等寬字型嚴格收斂至真實的代碼、金鑰與 Token。
  2. **介面文字降噪與去 AI 模板感（UI De-noising & De-AI-ification）**：中文介面中機械性地附加英文括弧（如 `API Keys`、`Recent Measurements`、`Scopes`、`Job ID`、`Showcase`），是典型的 AI 模板未精修痕跡，不僅分散注意力且降低專業度。回歸純粹的繁體中文操作介面，並以標準的淺灰微邊框表頭與語意化狀態標籤呈現數據，能顯著提升企業級 SaaS 的成熟度。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 開啟瀏覽器確認三頁面在桌機與行動端的字體渲染效果與中英混排邊界。
- **下次遇到類似問題我會怎麼做**：在建立多端或多產品線介面時，從一開始就確立全域 CSS Token 的階層，防止子頁面自行定義獨立的字體或按鈕規則。

---

### 2026-09-10 產品 A/B 雙儀表板未登入嚴格防禦（Auth Gate Barrier）與跨頁導航統一

- **情境**：使用者要求「品牌規範去掉，展示首頁換成回到首頁（其他子分頁也一樣），並且產品 A、B 的 Dashboard 都加上登入按鍵，不允許未登入使用；前端先做，具體串 Google API 的部分後續再用 computer use 串接」。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **認證閘門模式（Client-Side Auth Gate Barrier & Zero Unauthenticated Leak）**：「不允許未登入使用」不能僅靠非同步跳轉或 Toast 警告，因為那容易造成內容閃爍（Flash of Unauthenticated Content）。在視圖渲染最上層判定認證狀態：若無有效會話憑證，工作區或分頁內容完全不掛載至 DOM，改為渲染高質感的登入守門卡片（Auth Gate），並提供即時憑證輸入與示範帳號一鍵體驗（Demo Account），既符合高標準資安隔離，又保證驗收流程的絲滑流暢。
  2. **D-042 權限隔離架構的前端嚴格對齊**：產品 A（行銷儀表板）採用行銷端專屬會話憑證（`gds_`），程式碼內嚴格禁止出現任何開發者金鑰或租戶術語（`gck_`、`gcs_`、`api_key`）；產品 B（開發者控制台）採用開發者會話憑證（`gcs_`），登入後方可管理 API 金鑰與近期任務，兩套產品憑證與心智模型徹底分立。
  3. **導航統一性與心智模型校正**：將各子頁面雜亂的「展示首頁」、「產品首頁」全面收斂為統一標準的「回到首頁」（指向根目錄 `/`），並自導航列中剔除與開發者無直接關聯的「品牌規範」，大幅降低認知負荷。
  4. **Google API / GA4 整合的前端先行解耦（Frontend Pre-wiring for Agentic Computer Use）**：將複雜的第三方服務串接拆分為「前端互動原型」與「後續環境授權（Computer Use 操作 Google Cloud Console）」。前端先將滑出式抽屜中的 Google 帳號授權按鈕、GA4 網站資源勾選清單與匯入流程完整就緒，使使用者與後續 Agent 能在清晰的規格下接續完成 OAuth 與 API 串接。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 開啟產品 A 儀表板（未登入狀態）確認總覽與分析模組皆被守門卡片安全防護，點擊示範登入後能流暢切換進入完整數據。
  - 開啟產品 B 控制台（未登入狀態）確認金鑰建立按鈕與任務清單皆處於鎖定狀態，登入後可順暢管理 API 金鑰。
  - 驗證開發者子頁面的「回到首頁」按鈕是否皆能正確返回根目錄。
- **下次遇到類似問題我會怎麼做**：在面臨第三方 API 串接等需要外部審查或環境憑證的任務時，優先實作高保真的前端交互介面與資料模型（Mock/Fixtures），使 UX 流程能立即進行視覺驗收，再行對接真實外部驗證。

---

### 2026-09-10 登入流程現代化：空白畫布置中防禦、Google 官方 GIS 規範與全面淘汰邀請制

- **情境**：使用者指出原本「請先登入」畫面排版奇怪（左側側邊欄依然顯示「載入中...」與導航項目，登入卡片被塞在右側內容區且要求輸入邀請碼 `gdi_...`）。要求：未登入時應該是直接乾淨的空白背景、登入區塊完全置中；右上角登入按鈕與畫面中央登入鈕必須符合 Google 官方按鈕標準設計；目前 GeoCheck 只接受 Google 登入，並非邀請制。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **未認證狀態的乾淨全幅畫布（Unauthenticated Blank Canvas & Sidebar Concealment）**：未登入時若保留左側邊欄，會向使用者暴露未授權的品牌列表與「載入中...」狀態，產生介面破損的視覺錯覺。透過全域 `.app-shell.unauthenticated-state` 將側邊欄徹底隱藏（`display: none !important`），並將右側內容區延展為 100vw 的純白畫布（`#FFFFFF`），使用 Flexbox `align-items: center; justify-content: center` 將登入卡片精準居中，營造現代頂級 SaaS（如 Linear、Notion、Supabase）的開箱沉浸感。
  2. **Google Identity Services (GIS) 官方按鈕標準實作**：Google 官方對登入按鈕有嚴格的設計規範（Logo 4 色順序、標準圓角邊框 `#DADCE0`、字體 500 字重「使用 Google 帳戶登入」以及懸浮陰影 `0 1px 2px rgba(60,64,67,.3)`）。在頂部導航列右上角與中央卡片全面套用符合規範的原生按鈕元件，避免手刻非標準樣式帶給使用者的山寨感或不信任感。
  3. **產品註冊與身份驗證的心智模型收斂（OAuth Direct Login vs. Invitation Token Overhead）**：淘汰由管理員手動核發邀請碼（`gdi_...`）的過渡設計，全面收斂為 Google 帳戶單一登入。前端一鍵觸發或點擊「體驗示範帳號」即可直接進入工作區，大幅降低新用戶的摩擦力，並為後續 Computer Use 串接真實 Google Cloud OAuth 2.0 提供標準接口。
  4. **用戶端介面嚴格剔除內部測試控制器（Purging Development Fixture UI from Production Shell）**：側邊欄底部曾遺留「驗收沙盒模式」、「切換線上」、「FRONTEND_ACCEPTANCE 5 大場景即時預覽」等內部測試控制項。這類偵錯元件若流入正式介面，會嚴重削弱商業 SaaS 的專業度。將其徹底移除，替換為極簡的「Google 帳戶已連線」狀態卡與版本標註，使側邊欄回歸 100% 商業產品水準。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 開啟未登入狀態之 Product A Dashboard（`/app`），確認側邊欄已完全隱藏，畫面呈全白置中卡片，右上角及中央皆為標準 Google 登入按鈕。
  - 點擊任一 Google 登入按鈕，驗證能無縫登入並展示完整儀表板。
  - 檢視登入後的左側側邊欄底部，確認所有「驗收沙盒模式」、「5 大場景」等測試文字已全數消失，改為極簡 Google 帳戶連線狀態卡。
- **下次遇到類似問題我會怎麼做**：在設計產品登入/守門頁面時，將「未認證狀態」視為獨立的 Layout 情境處理，而非僅僅在既有的後台骨架裡更換局部文字；內部驗收工具（如 Fixture 切換）應透過 DevTools、隱藏快捷鍵或獨立測試入口調用，切勿直接放置於正式使用者的側邊欄中。

---

### 2026-09-10 主分支快進合併與雙產品線（行銷儀表板 vs 開發者模式）接口串聯架構

- **情境**：使用者要求將當前所有已完成的工作（`codex/cloudflare-full-migration` 分支）合併至主分支（`main`），並推送到遠端倉庫；同時思考與處理產品 A（行銷儀表板 `/app`）與產品 B（開發者模式 `/developers`）在後端 API 路由掛載與前端介面接口上的串聯與顯示方式。
- **核心概念**（Agent 版本，**請用自己的話改寫後才算數**）：
  1. **D-042 權限隔離架構下的跨產品導航串聯（Cross-Product Discovery without Security Leak）**：
     - 行銷人員使用 `/app` 關注品牌在 AI 搜尋中的能見度、引用率與官網連結；技術人員或工程團隊使用 `/developers` 關注 REST API、TypeScript/Python SDK、多模型並行探測與 Webhook 自動化。
     - 兩者在資安邊界上絕對隔離：`/app` 僅持有行銷會話憑證（`gds_`），絕不碰觸開發者金鑰（`gck_`）或租戶管理；`/developers/console` 僅持有開發者憑證（`gcs_`）或 API Key。
     - 在使用者介面層面，透過在產品 A 側邊欄優雅提供「開發者生態：開發者模式 (API & Docs) ↗」外部連結，並在產品 B（Showcase、Docs、Console）頂部導航列提供「行銷儀表板 ↗」，實現無縫導流與產品雙輪驅動，互不侵蝕權限邊界。
  2. **路由掛載強健性與別名相容（Route Aliasing & OAuth Redirect Integrity）**：
     - 在後端 HTTP 路由器（`services/api/server.js`）中，除了支援標準路徑 `/developers/console` 外，補齊 `/developers-console` 別名掛載，防止舊鏈接或外部回調產生 404。
     - 同時將 Google OAuth HTTP 處理器（`google-oauth-http.js`）的開發者認證重定向標準化為 `/developers/console`，保證 OAuth 登入成功後精準引導至控制台首頁。
  3. **非互動環境下的 Git 認證守則（Non-Interactive Git & Credential Lifecycle）**：
     - Windows 上的 Git Credential Manager (GCM) 在終端非互動模式（或 Background Task）下，若 Token 過期（如 GitHub PAT / `gh auth status` 顯示無效），會因嘗試彈出視窗或等待終端輸入而導致背景任務永久掛起（Hang）。
     - 透過設定 `$env:GIT_TERMINAL_PROMPT=0` 與 `$env:GCM_INTERACTIVE="never"` 可精確識別憑證錯誤並防止背景卡死；對於主分支合併，在本地以 `--ff-only` 完成無損快進合併，並明確告知使用者執行 `gh auth login` 刷新權限後完成推送。
- **我能解釋到什麼程度**：〔待自評〕
- **仍需人工驗證**：
  - 於本機或測試環境點擊 `/app` 側邊欄「開發者模式 ↗」，驗證是否順暢在新分頁開啟 `/developers`。
  - 於 `/developers/console` 與 `/developers/docs` 點擊「行銷儀表板 ↗」，驗證是否正確切換回 `/app`。
  - 執行 `gh auth login -h github.com` 刷新 GitHub 憑證後，執行 `git push origin main`。
- **下次遇到類似問題我會怎麼做**：在處理背景執行之 `git push` 時，先以快速的唯讀或狀態檢查確認遠端認證狀態，避免因圖形驗證提示導致任務阻塞。
